import './Header.css';
import NavBar from './NavBar/NavBar';

const Header = () => {
    return (
        <header className="hero">
            <NavBar />
        </header>
    );
}
export default Header;
